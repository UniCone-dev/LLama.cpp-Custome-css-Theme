/* ==========================================
   PREMIUM MAC / SCI-FI THEME FOR WEBUI
   ========================================== */

/* Root Variables & Colors */
:root {
  --glass-bg: rgba(18, 18, 24, 0.65);
  --glass-sidebar: rgba(12, 12, 18, 0.75);
  --glass-border: rgba(255, 255, 255, 0.08);
  --accent-glow: rgba(59, 130, 246, 0.5);
  --accent-color: #3b82f6;
  --text-main: #f3f4f6;
  --text-muted: #9ca3af;
  --blur-intensity: blur(20px);
}

/* Overall Body Background (Dark Sci-Fi Backdrop) */
body, html {
  background: radial-gradient(circle at 50% 10%, #111827, #030712) !important;
  color: var(--text-main) !important;
  font-family: -apple-system, BlinkMacSystemFont, "SF Pro Display", "Segoe UI", Roboto, sans-serif !important;
}

/* Sidebar Styling - Mac Style Frosted Glass */
#sidebar, div[class*="sidebar"] {
  background: var(--glass-sidebar) !important;
  backdrop-filter: var(--blur-intensity) (-webkit-backdrop-filter: var(--blur-intensity)) !important;
  border-right: 1px solid var(--glass-border) !important;
  box-shadow: 5px 0 25px rgba(0, 0, 0, 0.5) !important;
}

/* Main Chat Container / Cards */
div[class*="chat-container"], div[class*="card"] {
  background: var(--glass-bg) !important;
  backdrop-filter: var(--blur-intensity) !important;
  border: 1px solid var(--glass-border) !important;
  border-radius: 16px !important;
  box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37) !important;
}

/* Premium Buttons - Subtle Sci-Fi Glow */
button {
  border-radius: 10px !important;
  background: rgba(255, 255, 255, 0.05) !important;
  border: 1px solid var(--glass-border) !important;
  backdrop-filter: blur(10px) !important;
  color: var(--text-main) !important;
  transition: all 0.3s ease !important;
}

button:hover {
  background: var(--accent-color) !important;
  box-shadow: 0 0 15px var(--accent-glow) !important;
  border-color: rgba(255, 255, 255, 0.3) !important;
  transform: translateY(-1px);
}

/* Smooth & Minimalist Mac Scrollbar */
::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

::-webkit-scrollbar-track {
  background: transparent;
}

::-webkit-scrollbar-thumb {
  background: rgba(255, 255, 255, 0.15);
  border-radius: 20px;
  backdrop-filter: blur(5px);
}

::-webkit-scrollbar-thumb:hover {
  background: rgba(255, 255, 255, 0.3);
}

/* Input Box Styling */
textarea, input[type="text"] {
  background: rgba(0, 0, 0, 0.4) !important;
  border: 1px solid var(--glass-border) !important;
  border-radius: 12px !important;
  color: #fff !important;
  backdrop-filter: blur(10px) !important;
  transition: border 0.3s ease, box-shadow 0.3s ease !important;
}

textarea:focus, input[type="text"]:focus {
  border-color: var(--accent-color) !important;
  box-shadow: 0 0 12px var(--accent-glow) !important;
  outline: none !important;
}
